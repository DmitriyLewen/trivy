name: maximize-build-disk-space-test
on:
  workflow_dispatch:

jobs:
  maximize-build-disk-space-test:
    name: maximize-build-disk-space-test
    runs-on: ubuntu-latest
    steps:
      - name: "free size"
        run: |
          echo "size of /home"
          du -sh /home/* 2>&1 | grep -v  '^du:'
          echo "size of /home/runner"
          du -sh /home/runner/* 2>&1 | grep -v  '^du:'
          echo "size of /home/runner/runners"
          du -sh /home/runner/runners/* 2>&1 | grep -v  '^du:'          
          

      - name: Maximize build space
        uses: easimon/maximize-build-space@v7
        with:
          root-reserve-mb: 35840 # Required for Goreleaser
          remove-android: 'true'
          remove-docker-images: 'true'
          remove-dotnet: 'true'

      - name: Checkout code
        uses: actions/checkout@v3.5.3
        with:
          fetch-depth: 0

      - name: "free size"
        run: |
          df -h
          echo "tmp folder:"
          df -h /tmp       
